/** Layout tokens — mirrors Mobile-production `shop_tokens.dart`. */
export const BOTTOM_NAV_EMPTY_STATE_CLEARANCE = 88;
export const BOTTOM_NAV_HEIGHT = 64;

export const FLASH_GRADIENT = 'linear-gradient(135deg, #FF6B6B 0%, #FF8E53 100%)';

/** Live dropping price — distinct from brand primary purple. */
export const LIVE_PRICE_TEXT_CLASS = 'text-emerald-600 dark:text-emerald-400';

/** Horizontal rail card width — matches deals promotion rail / mobile product rail. */
export const PRODUCT_RAIL_CARD_CLASS = 'w-[min(280px,78vw)] flex-shrink-0 snap-start';

/** Sort `<select>` — wide enough for Arabic labels (e.g. السعر الأسرع انخفاضاً). */
export const SORT_SELECT_CLASS =
  'input h-10 w-auto shrink-0 min-w-[12.5rem] sm:min-w-[14rem] max-w-[16rem] text-sm py-2 pe-9 cursor-pointer';
